# Source Evidence For /gen-env、Harness PR1 与 ASPIRE 研究落点

这是一份审计材料，不是读者首页。如果行为还看不清楚，先读 [一条真实路径](../walkthroughs/one-real-run.md)。

核心 `/gen-env` 路径证据基于 commit `60a2597` 收集；Harness PR1 schema 证据基于实现
commit `9b72090` 补充。

被追踪的工作行为：用 `python script/generate_scene.py --prompt "Place a can on top of a plate." --seed 42 --asset-catalog data/scene_gen/asset_catalog.json --out-root data/generated_scenes` 编译出哈希绑定的 resolved 场景包，再用 `python script/run_scene_runtime.py` 把这个包在 RoboTwin/SAPIEN 里物理回放并采到结构化运行时证据。

## Evidence Traversal Log

| Pass | Purpose | Inspected evidence | What changed in the model |
| --- | --- | --- | --- |
| Pass 1 | 把主路径从 prompt 端走到运行时证据端。 | `README.md`、`script/generate_scene.py`、`script/run_scene_runtime.py`、`scene_gen/parser.py`、`scene_gen/schema.py`、`scene_gen/grounding.py`、`scene_gen/solver.py`、`scene_gen/builder.py`、`scene_gen/validator.py`、`scene_gen/envs/generated_scene.py`、`scene_gen/runtime_sampling.py`、`tests/fixtures/prompt_matrix.json`、`scene_gen/asset_overrides.yml`。 | 五段主路径成形：受限解析 → 类型化契约 + grounding → 目标局部求解 → 哈希绑定包 → SAPIEN 回放采集 + 运行时门控。catalog miss/collision 不稳走确定性代理。 |
| Pass 2 | 用读者怀疑视角挑战模型，补边界与失败分支，盘清在范围内的源码区。 | `scene_gen/asset_generator.py`、`scene_gen/support_geometry.py`、`scene_gen/acceptance.py`、`scene_gen/runtime_sampling.py`、`tests/scene_gen/test_builder_validator.py`（攻击用例清单）、`tests/scene_gen/test_solver.py`、`tests/scene_gen/test_parser.py`、`tests/scene_gen/test_schema.py`、`docs/evidence/prompt-matrix-20260719.md`、`docs/evidence/physics-acceptance-20260717.md`、`demo/app.py` 的环境变量与队列层。 | 模型补齐：求解器是有界拒绝回退、失败带机读 trace；嵌套源必须动态且接触声明 target ≥ 80% 终末窗口、且对未声明 target 零接触；120 帧视频 = 119 释放帧 + 1 终末帧、≥ 30 帧互异；固定物体 translation drift ≤ 20 mm、rotation drift ≤ 3°，resolved pose error ≤ 20 mm / 5°；derived proxy 携来源 lineage；`acceptance.py` 只做 ≥95% 通过率聚合；renderer 不是物理证据。锁定源码区为 `scene_gen/`、`script/`、`demo/`、`tests/`，并标出排除项。 |
| Pass 3 | 扩展到 Harness PR1 的类型边界，并挑战“schema 已存在即 Skill 可执行”的误读。 | `self_improving/harness/schemas/base.py`、`common.py`、`text2env.py`、`schema_catalog.py`、14 份 `json_schemas/*.schema.json`、`tests/self_improving/harness/`、`script/export_harness_schemas.py`、`script/run_self_improving_tests.sh`、`pyproject.toml`。 | 在 PR1 当时：14 个严格/frozen 公共入口只冻结接口与局部自洽；`ArtifactRef` 引用既有权威载荷；RunState 执行成功不等于验证通过；Registry、handler、invocation digest、内容解析与 MCP 尚未实现。后续 resolver/recorder 见 Pass 5。 |
| Pass 4 | 用 ASPIRE 的 trace、验证后技能晋升和 held-out 评估思想挑战证据声明与状态机。 | `scene_gen/validator.py`、`tests/scene_gen/test_builder_validator.py`、Stage 5 main/legacy/critic/batch、`test_review_state.py`、`self_improving/studies/ASPIRE/`、固定子模块 `external/ASPIRE@7ba73d3`。 | 增加 runtime evidence 自声明 scene/digest equality checks；把 active pending、batch aggregate 和默认 static-only 与 final/pass 分离；合成 E2 只保留为候选机制。 |
| Pass 5 | 同步研究期间并发落地的 Harness follow-on，挑战“digest check 即安全 capability”的误读。 | `self_improving/harness/artifacts.py`、`events.py`、`event_journal.py`、`registry.py`、`assets.py`、`IMPLEMENTATION_LOG.md`、对应 tests，以及 `scene_gen/compiler.py`。 | 已有本地 artifact resolver、callback-driven RunRecorder、SQLite append-only journal、通用 qualified-version Registry 和可导入 compiler；`28333de` 仍无 Text2Env handlers/MCP。`file://` 无 allowed-root、校验后路径可变，不能称 sandbox/不可变 snapshot；asset generation-QC 未跑 settle，不能标成物理 verification。 |
| Pass 6 | 跟进首个 Text2Env compile 竖切，并用 check/use mutation 与失败后副作用挑战“输入冻结/原子晋升”说法。 | `284ffb` compile adapter；`1a1f3d8` PackageStore；`e98e8c1`/`5915315` dependency binding；`ef5e29e`/`910ccb1` catalog attack/fix；`scene_gen/compiler.py` 与 `assets.py`。 | 原 handler 的 catalog copy 后仍回读 locator，mutation 可绕过 roots；冻结攻击后修为只消费 CAS catalog，Harness 74 pass/100% coverage。asset admission 仍在 solve 前且 blocked 后不回滚；replay/validate/MCP 仍缺。 |
| Pass 7 | 继续攻击“CAS ref 等于同一份 bytes”和“receipt 声称报告存在即可信”。 | `50e8f18:artifacts.py`、`51447da:registry.py`、对应 artifact/Registry tests 与 implementation log。 | CAS put 现单次流式 hash+copy、fsync、原子 rename并拒绝已污染目标；Registry 现要求 qualification `report_sha256` 的 CAS bytes 存在且匹配。报告领域内容/source/handler 对账仍未闭合。 |
| Pass 8 | 在干净固定源码上反查实现日志与移动工作树，避免环境残留制造绿色覆盖率。 | `git archive 51447da` 的 root/importlib/Harness pytest，以及同提交 detached clone 的 canonical platform script。 | 默认 collection error；importlib 184/1；Harness 76 pass 但 99.88% coverage，未覆盖 bytecode/cache 排除分支。实现日志的 100% 不能作为 clean receipt。 |
| Pass 9 | 检查 CLI 契约修复后的最终固定源码，而不把独立 replay follow-on 倒灌为 ASPIRE 结果。 | `e6ed0ff`、`148001d` 与 `git archive 148001d` 的 root/importlib/Harness pytest。 | importlib 201/0，旧 CLI 漂移已闭合；默认 collection 与 Harness 99.88% coverage 仍阻断全绿。replay timeline receipt 未作为 matched repair trial 重跑。 |
| Pass 10 | 审计封存期间并发落地的 qualification、packaging 与 RunStore，挑战“前置构件存在即 P0 闭环”的误读。 | 固定 `4836ebf`、`38518e5`、`ab03859` 源码/tests/log，以及 `git archive ab03859` Harness/root pytest。 | 三者分别是预制 bundle verifier、wheel prerequisite、immutable terminal-record adapter；固定树无 Registry callsite，三项 P0 均 open。Harness 133 pass/99.91%，默认 collection 仍失败。 |
| Pass 11 | 挑战“扩展名、worker JSON 或可播放 preview 即可信 replay media”的误读。 | `media_sandbox.py`、`native/media_sandbox.c`、`media_verifier.py`、对应攻击测试与 `docs/evidence/replay-media-verifier-qualification-20260831.*`。 | 媒体 consumer 在 delegated cgroup/Landlock/seccomp/rlimit 中以静态 FFmpeg 从 held FD 完整解码；真实历史 MP4 为 120 frames / 114 decoded unique。该证据只资格化 consumer，handler 接线与正式 900/120 replay/validate/promotion 仍 open。 |
| Pass 12 | 检查 replay handler 是否真实消费 Invocation identity，而不是靠 resolver side channel 或 worker 自述晋升产物。 | `handlers/text2env_replay.py`、`replay_dependencies.py`、两份专项测试、`docs/evidence/replay-handler-integration-20260831.*`。 | 候选 adapter 精确绑定 capability/executor/handler-config/media-verifier/runtime-assets 五项依赖，在执行前后独立重算并对账；JSON/PNG/MP4 先以 untrusted bytes 入 CAS，事件、evidence、validation 和固定视频 metadata 全过后才 typed promotion。专项 185 passed、两个模块 statement/branch 100%；固定 qualification/application、新的 900/120 handler 真跑和 descriptor 确定性语义仍 open。 |
| Pass 13 | 反证“ledger 标 v3 且作者自报 files 即内容完整”，并遍历所有 ACTIVE 写入口。 | `lib/ledger.py`、`ledger_writes.py`、migrate/backfill/materialize/runtime/repair/writeback writers、generated admission、攻击测试和 `docs/evidence/asset-ledger-v3-integrity-20260831.*`。 | v3 gate 现解析 loader closure、复核每个 member 与 provenance/receipt，并由共享写入边界 fail closed；asset-reuse 602 pass/2 skip、跨 Harness 102 pass。只读审计 162/162 旧 ledger 有 4,082 条债务，未被批量改写或伪造物理 pose。 |
| Pass 14 | 反证“同输入的物理 Skill 必须逐字节确定”并检查旧 schema 是否被静默改义。 | v1/v2 descriptor models、Registry union、compile/replay factories、15 份 snapshot、`docs/evidence/harness-skill-descriptor-v2-20260831.*`。 | v1 snapshot SHA 前后相同且仍只允许 true；v2 明确区分 content-bitwise 与 evidence-invariant repeatability，Registry 不转换。专项 209 pass/100%；这不构成 replay qualification。 |

Coverage note：本证据图覆盖在范围内的 `scene_gen/`、`script/`、`demo/`、`tests/`
主路径，以及 `self_improving/harness/` 的 PR1 schema tranche。已检查但不追踪的相邻路径——
`scene_gen/rendered_critic.py` 与 `script/run_rendered_critic.py` 的 VLM 渲染评判（非物理证据、
可选 extra）、`script/run_100_seed_acceptance.py` 与 `script/run_prompt_matrix.py` 的批量/矩阵
runner（主路径之上的薄编排）、`script/build_stage5_report.py` 的报告聚合、`demo/app.py` 的
Flask 控制面（消费同一 `scene_gen` 流水线但不是编译路径本体）——都在
[代码地图](../code-map.md) 里登记为在范围但只摘要，不重复追踪。artifact resolver、run recorder、
PackageStore、通用 Registry、compile adapter、独立 validate handler、媒体 consumer 与 replay
handler/resolver 候选已有实现；replay 固定 production qualification/application、正式 900/120
handler receipt 仍未完成；descriptor 已版本化为 evidence-invariant repeatability，但尚无对应 pass
bundle。跨 run qualification/promotion transaction 与
MCP adapter 也未闭合，不能从一个 compile seam、一段可解码视频或单元绿灯反推完整 Text2Env
物理发布行为。

| Claim | Evidence | Confidence | Caveat | Used by |
| --- | --- | --- | --- | --- |
| 解析器把受限中英 prompt 编译成类型化 `SceneSpec`，绝不产出代码/路径/id/pose。 | `scene_gen/parser.py:parse_rule_based`、`FORBIDDEN_PROMPT_PATTERNS`、`scene_gen/schema.py:FORBIDDEN_SCENE_KEYS`、`tests/scene_gen/test_parser.py` 中 `test_provider_payload_cannot_smuggle_backend_fields_or_change_request`、`test_all_invalid_golden_prompts_are_rejected`。 | Confirmed | 解析器是规则式（正则 + 词典），不是开放式 LLM；`StructuredSceneProvider` 协議存在但默认 CLI 走 rule-based。 | bounded-parser, scene-contract, walkthrough |
| `SceneSpec` 用 pydantic 严格契约（`extra="forbid"`、`frozen=True`），跨字段语义由 `model_validator` 保证；`ResolvedSceneSpec` 通过 `source_scene_spec_sha256`/`asset_catalog_sha256` 与上游哈希绑定。 | `scene_gen/schema.py:SceneSpec`、`ResolvedSceneSpec`、`SceneSpec.semantic_consistency`、`ResolvedObject.stable_pose_plus_yaw_only`、`SceneSpec.digest`、`ResolvedSceneSpec.digest`。 | Confirmed | 字段跨约 50 个；完整的字段级意义在模块里说，不在表里重复。 | scene-contract, replay-package, walkthrough |
| grounded 资产选择按语义分（category / alias / semantic_name）+ color/material 元数据筛，按确定性 tie-break 排序，被拒候选保留在 `rejected_candidates`。 | `scene_gen/grounding.py:ground_object`、`_semantic_score`、`_tie_break`。 | Confirmed | color/material 在 catalog 空时不参与过滤，只保留 query。 | scene-contract, target-local-geometry, walkthrough |
| 目标局部几何由声明的 target 计算：support 取 `support_surface_*`，containment 取 `interior_dimensions_m` + `interior_floor_z_offset_m`；不读外层 AABB。 | `scene_gen/support_geometry.py` 中 `support_surface_dimensions`、`support_surface_shape`、`support_surface_z`、`support_footprint_margin`；`scene_gen/asset_overrides.yml` 中 `003_plate` 的 100 mm 稳定面 + 8 mm 余量、`110_basket` 的 12 mm 内底。 | Confirmed | override 条目须有实测几何或文档化仿真器探测——不许编造尺寸。 | target-local-geometry, solver, runtime-gates |
| 求解器是 bounded rejection backtracking：每物体最多 96 次尝试、最多 48 次回退；失败抛 `SceneSolveError` 带机读 trace；按 support depth + degree + id 排序摆放。 | `scene_gen/solver.py:solve_scene`、`place` 内部闭包、`max_attempts_per_object`/`max_backtracks` 默认值。 | Confirmed | 攻击用例 `test_impossible_workspace_fails_with_bounded_machine_readable_trace` 不直接跑（已被后续 test_solver.py 文件 audit 但本表未引行）。 | solver, target-local-geometry, walkthrough |
| catalog miss 或 collision 不稳时由确定性代理替代：procedural primitive 或 derived uniform scale；代理写 `generation_provenance.json` 携来源 lineage；自动 scale 仅限已通过真实回放的 block/cube 类。 | `scene_gen/asset_generator.py`、`PRIMITIVE_PROXY_SOURCES = {("004_fluted-block", 0)}`、`SCALE_HEADROOM = 0.92`、`MIN_DERIVED_SCALE = 0.35`；`tests/scene_gen/test_asset_generator.py` 中 `test_incompatible_block_is_derived_at_a_uniform_scale_and_fits_plate`、`test_runtime_unstable_block_uses_dimension_preserving_proxy_on_table`。 | Confirmed | 自动兼容缩放当前只限已验证的 block/cube；扩类须先过真实 SAPIEN 回放。 | derived-proxy, walkthrough |
| `build_scene_package` 写 `request.txt`、`scene_spec.json`、`resolved_scene.json`、`generated_scene.py`、`package_manifest.json`，并对每个文件 + resolved canonical digest 算 SHA-256；`verify_package` 重新核对。 | `scene_gen/builder.py:build_scene_package`、`verify_package`、`_sha256`。 | Confirmed | 攻击用例 `test_package_verifier_detects_tampering` 锁住篡改检测。 | replay-package, walkthrough |
| 回放入口只按 resolved 字段构建 SAPIEN actor，不执行任何用户代码；颜色 override 通过 material；articulation 用 `set_qpos` + drive 属性。 | `scene_gen/envs/generated_scene.py:load_resolved_scene`、`_apply_color_override`、`if item.articulation_qpos:` 块。 | Confirmed | 仅 CI 验证可导入表面；真实回放需 RoboTwin checkout。 | runtime-gates, replay-package, walkthrough |
| 运行时按 `total_steps = max(settle_steps, video_frames)` 演进；视频采样 119 释放帧 + 1 终末帧；已验证配方显式用终末 120 步采集 support / unexpected contact；终末状态读 drift、resolved 误差、可见像素、穿透、是否仍在动、是否掉落。 | `script/run_scene_runtime.py:main`、`scene_gen/runtime_sampling.py:video_sample_steps`、`runtime_support_margin`、`runtime_inside_contained`。 | Confirmed | 独立 CLI 的 `--contact-window-steps` 默认是 60，README/矩阵/本指南命令显式传 120；`--precheck-steps 0` 默认不预步进，避免隐藏初态不稳。 | runtime-gates, walkthrough |
| validator 先比较 runtime evidence 自声明的 `scene_id`/resolved digest，再把物理字段转成 checks：固定物体 translation drift ≤ 20 mm、rotation drift ≤ 3°、resolved pose error ≤ 20 mm / 5°；动态物体按 contact fraction ≥ 0.8、未声明 target 零接触、视频 ≥ 30 互异帧、可见像素 ≥ 64、不穿透、不仍在动、不掉落、articulation qpos 误差 ≤ 0.02。 | `scene_gen/validator.py:validate_resolved_scene` 中 `runtime_scene_identity`/`runtime_resolved_scene_binding`、四个 drift/error 默认值及物理 checks；`tests/scene_gen/test_builder_validator.py:test_runtime_validator_requires_each_object_visibility_and_physics`。 | Confirmed | 缺失/未改写错值会 fail；搬运别次 payload 后重写两项声明仍可能过门。未绑定完整 evidence/media/command，不证明真实归属。 | runtime-gates, walkthrough |
| 渲染不是物理证据；`rendered_critic` 只查可见语义，确定性物理门控才是权威。 | `scene_gen/rendered_critic.py`、`tests/scene_gen/test_rendered_critic.py`、根 `AGENTS.md` 中「渲染不是物理证据」契约。 | Confirmed | VLM 是可选 extra（`pip install -e '.[vlm]'`）。 | runtime-gates, glossary |
| 真实回放已知通过：2026-07-19 prompt matrix 33/33 编译、10/10 SAPIEN 通过、每段 MP4 ≥ 100 互异帧。 | `docs/evidence/prompt-matrix-20260719.md`、`docs/evidence/prompt-matrix-runtime-20260719.json`、`docs/evidence/physics-acceptance-20260717.md`。 | Confirmed | 该证据为外部 GPU 主机一次性运行结果，非 CI；当前未在本仓库自动重跑。 | walkthrough, runtime-gates, quality-review |
| `pytest -q` 无需 RoboTwin checkout 即可跑；契约改动另需真机回放。 | `pyproject.toml`、`tests/`、根 `AGENTS.md` 「Testing Requirements」、`tests/fixtures/` 是 committed fixture。 | Confirmed | demo 与运行时路径不在 CI 跑真实 GPU；由中国 AGENTS.md 协议要求在支持 RoboTwin/SAPIEN 的机器上补做真机回放。 | walkthrough, code-map, every module 的 verify 块 |
| Harness PR1 公开 14 个严格、不可变 schema，并用只读 catalog 与 committed snapshot 锁住 `$id` 和字段形状。 | `self_improving/harness/schemas/base.py:HarnessModel`、`schema_catalog.py:SCHEMA_MODELS`/`schema_documents`/`export_schema_snapshots`、`json_schemas/`、`tests/self_improving/harness/test_schema_catalog.py`。 | Confirmed | Pydantic `model_validator` 不会全部翻译成纯 JSON Schema；跨字段语义仍以进程内模型为准。 | harness-schema-tranche, code-map |
| `RunState` 只接受 running 到 succeeded/blocked/failed 的生命周期，事件必须连续并与 attempt/时间/终态自洽；succeeded 只表示得到类型化 output。 | `self_improving/harness/schemas/common.py:RunState`、`tests/self_improving/harness/test_common_schemas.py` 的 running、terminal、retry 与破坏历史用例。 | Confirmed | 预检 `attempt=0/max_attempts=0` 是 PR1 实现选择，仍待 RFC Accepted 前确认。 | harness-schema-tranche, glossary, quality-review |
| Text2Env schema 复用现有 `robotwin.*` 载荷权威，锁定 replay 默认值，并要求只有 `validation_status=pass` 且无 blocker 才能表达 `publishable=true`。 | `self_improving/harness/schemas/text2env.py:RuntimeConfig`/`EnvironmentPackage`/`Text2EnvValidateOutput`、`tests/self_improving/harness/test_text2env_schemas.py`、`test_schema_catalog.py`。 | Confirmed | compile adapter 已存在；跨 compile/replay/validate、qualification、包内容哈希和物理 gate 的完整发布判断仍未实现。 | harness-schema-tranche, self-improving-platform, quality-review |
| `LocalArtifactStore` 可内容寻址写入并校验本地 artifact，`RunRecorder` 可由 callback 生成连续 event/RunState，`SQLiteEventJournal` 可持久化并按全局 cursor 重放 committed events。 | `self_improving/harness/artifacts.py`、`events.py`、`event_journal.py` 及对应 tests；`50e8f18` CAS attack/fix。 | Confirmed | `put_file` 已单次流式 hash+copy 并原子晋升；resolver 的任意 `file://` 仍无 root confinement且返回可变路径，CAS 也无 OS 只读位保证。journal 不补完整 package/run/media receipt。 | harness-schema-tranche, self-improving-platform |
| 通用 `SkillRegistry` 已按 exact version + qualification receipt 注册 handler，并校验类型、输入 artifact、依赖身份、invocation digest、attempt 与事件；`e98e8c1` 还把展开后的 effective parameters 交给 dependency resolver。 | `self_improving/harness/registry.py`、对应 tests、`IMPLEMENTATION_LOG.md:A006/A014/A018`。 | Confirmed | `51447da` 已要求 `report_sha256` CAS bytes 存在且匹配，但还不解释 report 内容、不对账 source manifest 或实际 handler bytes；compile resolver 在 `5915315` 已提交。 | harness-schema-tranche, self-improving-platform |
| `Text2EnvCompileHandler` 可在调用方显式组装 Registry 后执行真实 `compile_scene`、记录 stage events、收集 CAS artifacts 并复核 package bindings；catalog JSON 的 check/use 已绑定同一 CAS snapshot。 | `284ffb` handler、`ef5e29e` mutation attack、`910ccb1` fix 与 focused tests。 | Confirmed for catalog snapshot use | 攻击从 0/1 到 1/1，Harness 74 pass/100% coverage；它仍不是自动注册、sandbox 或完整 Text2Env 闭环，referenced asset payload 也未随 catalog JSON 一起重物化。 | harness-schema-tranche, self-improving-platform, code-map |
| `PackageStore` 可按 manifest 把成员发布进 CAS，并仅凭摘要在 staging 重建、`verify_package` 后晋升。 | `1a1f3d8:self_improving/harness/package_store.py` 与 `test_package_store.py`。 | Confirmed by source/tests in commit | 只覆盖 scene package bytes；不绑定 runtime evidence、媒体、命令或物理 gate。 | harness-schema-tranche, self-improving-platform, code-map |
| replay handler/resolver 候选只接受 capability、executor、handler config、media verifier、input-specific runtime assets 五项精确 Invocation 依赖；再次物化/快照并在执行前后对账，worker 输出经 evidence/validation/完整媒体 metadata 复核后才 typed promotion。 | `self_improving/harness/handlers/text2env_replay.py`、`replay_dependencies.py`、两份专项测试、`docs/evidence/replay-handler-integration-20260831.*`。 | Confirmed as candidate vertical slice | 185 tests 与双模块 100% statement/branch 是单元攻击证据；descriptor 已诚实声明 v2 evidence-invariant repeatability，但尚无固定 qualification/application、新的 900/120 handler receipt 或独立 promotion。 | runtime-gates, self-improving-platform, code-map |
| `qualification.py` 可严格核预制 pass receipt/report/manifest、manifest 已列实现文件和两个 source trees，再把 report/receipt 快照写入 CAS。 | `4836ebf:qualification.py:96-215` 与 `test_qualification.py`。 | Confirmed as static verifier | 固定树无 Registry callsite；不执行 regression command 或 development/clean cases；两个 publish 非 promotion transaction；implementation root 未列 helper 不受完整性证明。 | harness-schema-tranche, self-improving-platform, code-map |
| `SQLiteRunStore` 可不可变、幂等地保存 Invocation 与 terminal RunState，并对账同一 SQLite journal。 | `ab03859:run_store.py:52-184` 与 `test_run_store.py`。 | Confirmed as persistence adapter | 固定树无 Registry callsite；接受调用者 digest、无 running/resume、不会解引用 artifact bytes，不能称 identity-frozen EvaluationRun 或 package→run→media binding。 | harness-schema-tranche, self-improving-platform, code-map |
| 生成资产 admission report 保留 `physical_qualification=pending_settle`；ledger 的 `backend=sapien/check=generation_qc/verdict=pass` 已绑定最终 provenance 与 representation digest，但仍只表示 analytic generation QC。 | `self_improving/harness/assets.py:_build_ledger`、`GeneratedAssetAdmitter.admit` 与 `scene_gen/compiler.py` 的 admission→solve 顺序。 | Confirmed distinction | 该 ledger 项不是 SAPIEN settle receipt；admission 仍是 solve 前的 library side effect，后续 blocked 不回滚，不能称 compile-atomic promotion。 | self-improving-platform |
| asset-ledger v3 现在要求真实 loader 文件闭包、collision metadata、stable-pose provenance 和 current representation digest receipts；全部 ACTIVE writer 在发布前走 `check_files=True`。 | `active/1_asset_reuse/lib/ledger.py`、`ledger_writes.py`、各修改 writer 与攻击测试。 | Confirmed for implementation | 162/162 份既有 ledger 仍有 4,082 条内容债务，本切片未批量迁移；实现通过不等于旧资产数据已合格。 | self-improving-platform, code-map |
| Stage 5 active visual-review paths 的 pending 只保留 review candidate/exit 2；batch opt-in 只收集并聚合为 `review_required`；默认 static-only 写显式 candidate 与 not_run 字段。 | main/legacy pipeline、`scene_critic.py`、`run_scene_batch.py`，以及 `test_review_state.py` 的 pending、aggregate、pre-review crash、pass promotion、static-only 攻击路径。 | Confirmed | static-only 命令可 exit 0 表示阶段完成，但不是 final/物理 acceptance；smoke 和视觉 pass 仍不是核心物理证据。 | self-improving-platform |
| ASPIRE 风格的开发集验证、trigger-specific 冻结技能记忆在六类固定合成契约故障的 120 个 held-out cases 上 mHRC 为 1.00，current-trace-only arm 为 0.50。 | `self_improving/studies/ASPIRE/experiment_protocol.md`、`experiments/heldout_harness_benchmark.py`、`artifacts/heldout_harness_benchmark.json`、`results.tsv`。 | Confirmed for these fixed synthetic cases | 不含 LLM、真实 rollout 或物理变化；退化 bootstrap 区间不能估计统计总体或部署分布不确定性；预注册 Oracle ceiling 未实例化。 | self-improving-platform |

## 证伪检查

若 `python script/run_prompt_matrix.py --matrix tests/fixtures/prompt_matrix.json --runtime --robotwin-root /path/to/RoboTwin --out-root data/prompt_matrix --report data/prompt_matrix/report.json` 在某次主路径改动后不再 33/33 编译 + 10/10 SAPIEN 通过，本指南中「真实路径」字面通过率就不再对应当前源码——重跑该命令即可证伪。若 `pytest -q` 在改 `scene_gen/schema.py` / `parser.py` / `solver.py` 后出现新 fail，对应步骤里描述的契约已不准确，需回到主路径或对应模块同步源码反证。又一层反证：根 `AGENTS.md` 显式列出已被攻击测试锁住的误报模式（通过 `is_static` 堆叠、无接触 pose、外层 AABB 重叠、起止截图验收），若任一在 validator 改动后通过，则该误报保护已被反转，本指南 [运行时门控](../modules/runtime-gates.md) 攻击表也需同步更新。Harness 侧，`python script/export_harness_schemas.py --check` 的任何漂移或 Harness 覆盖率门失败，都会直接证伪“当前 15 个 committed schema 与 100% 语句/分支覆盖”的当前声明。

证据状态：除特别标注外，本页基于当前源码已确认。
